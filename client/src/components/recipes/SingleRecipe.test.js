import React from 'react';
import { shallow } from 'enzyme';
import { SingleRecipe } from './SingleRecipe';

describe('SingleRecipe', () => {
  const props = {};
  const singleRecipe = shallow(<SingleRecipe />);
});
